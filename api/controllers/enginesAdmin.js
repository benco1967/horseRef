'use strict';

// TODO
module.exports = {
  engines,
};

/**
 * Méthode pour /admin/engines
 *
 * Retourne le status courant de ce service
 * @param req request
 * @param res response
 */
function engines(req, res) {
  res.json([]);
}
